import { loadPlcTree, searchByDataType } from './plc';
import { expect, test } from '@jest/globals';

test('loadPlcTree', () => {
});

test('searchByName', () => {
});

test('searchByDataType', () => {
});

